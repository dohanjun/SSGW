<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.group.mapper.ModuleMapper">
	<select id="getAllModules" resultType="com.yedam.app.group.service.ModuleVO">
		SELECT 
	        MODULE_NO,
	        MODULE_NAME,
	        MODULE_PRICE,
	        VERSION,
	        BASIC_MODULE,
	        ACTIVATE,
	        EXPLANATION
	    FROM MODULE
    </select>
    
    <insert id="insertModule" parameterType="com.yedam.app.group.service.ModuleVO">
        <selectKey keyProperty="moduleNo" resultType="int" order="BEFORE">
	        SELECT nvl(MAX(MODULE_NO), 0) + 1 FROM MODULE
	    </selectKey>
	    INSERT INTO MODULE (
	        MODULE_NO,
	        MODULE_NAME,
	        MODULE_PRICE,
	        VERSION,
	        BASIC_MODULE,
	        ACTIVATE,
	        EXPLANATION
	    ) VALUES (
	        #{moduleNo},
	        #{moduleName},
	        #{modulePrice},
	        1.0,
	        'N',
	        'Y',
	        #{explanation}
	    )
	</insert>
    
</mapper>