<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.group.mapper.MailMapper">

	<!-- 메일전체조회 -->
	<select id="selectAllList" resultType="MailVO">
	SELECT   mail_id
	        ,title
	        ,content
	        ,sent_state
	        ,sent_date
	        ,employee_id
	        ,attached_file_name
	        ,get_user
	        ,cc
	        ,recode
	        ,mail_type
	FROM Mail
	ORDER BY mail_id
	</select>

	<!-- 메일단건조회 -->
	<select id="jej" resultType="MailVO">
	
	</select>
	
	<!-- 나의메일단건조회 -->
	<select id="agde" resultType="MailVO">
	
	</select>
	
	<!-- 메일등록 -->
	<insert id="wetew" parameterType="MailVO">
	
	</insert>
	
	<!-- 메일수정 -->
	<update id="saf" parameterType="MailVO">
	
	
	</update>
	
	<!-- 메일답장 -->
	<update id="dsfds" parameterType="MailVO">
	
	
	</update>
	
	<!-- 메일삭제 -->
	<delete id="dsf" parameterType="MailVO">
	
	
	</delete>
	
</mapper>